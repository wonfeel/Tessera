# Нормализация переносов строк: в репозитории — LF, в рабочей копии Windows — CRLF.
* text=auto

# Явно бинарные/неизменяемые форматы — не трогать.
*.png binary
*.lib binary
*.a binary

# Вендоренные зависимости: не учитывать в языковой статистике GitHub
# и сворачивать в diff/review. Репозиторий показывает код проекта, а не libs.
libs/** linguist-vendored
